package com.springbook.view.controller;

import java.util.HashMap;
import java.util.Map;

public class HandlerMapping {
	
	/*
	 * private Map<String, Controller> mappings;
	 * 
	 * public HandlerMapping() { mappings = new HashMap<String, Controller>();
	 * mappings.put("/login.do",new LoginController());
	 * mappings.put("/logout.do",new LogoutController());
	 * mappings.put("/getBoardList.do",new GetBordListController());
	 * mappings.put("/getBoard.do",new GetBordController());
	 * mappings.put("/insertBoard.do",new InsertBordController());
	 * mappings.put("/updateBoard.do",new UpdateBordController());
	 * mappings.put("/deleteBoard.do",new DeleteBordController()); }
	 * 
	 * public Controller getController(String path) { return mappings.get(path); }
	 */
}
